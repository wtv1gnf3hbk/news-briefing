Run the original (v1) briefing pipeline.

1. Run `node generate-briefing.js` to scrape all sources into briefing.json
2. Run `node write-briefing.js` to produce the briefing (single-shot, 1 API call)
3. Show the final briefing.md contents and timing
